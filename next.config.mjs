/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: '/NextJsSoloup',
    assetPrefix: '/NextJsSoloup',
    trailingSlash: true,
  };
  
  export default nextConfig;
  
